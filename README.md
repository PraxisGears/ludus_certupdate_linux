# ludus_certupdate_linux
Ansible Role for Ludus that simply installs ca-certificates on Linux VMs and uses it to perform a CA update

Created to overcome issues with some package installations encountering Certificate errors during a Ludus range build
